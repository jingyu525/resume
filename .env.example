# ── 埋点（GoatCounter，隐私优先，默认完全关闭）──
# 仅在下方填入 code 后才会加载统计脚本并发起请求；留空则零外部请求、不碰数据。
# 获取方式：在 https://www.goatcounter.com 注册并创建站点，得到 code（如 resume-studio）。

# GoatCounter 站点 code，例如 resume-studio（对应 https://resume-studio.goatcounter.com）
VITE_GOATCOUNTER_CODE=

# GoatCounter 脚本地址（默认官方 CDN；自建时改为自有域名下的 count.js）
VITE_GOATCOUNTER_SRC=https://gc.zgo.media/count.js
