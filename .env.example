# ── 埋点（隐私优先，默认完全关闭）──
# 仅在下方填入域名后才会加载 Plausible 脚本并发起请求；留空则零外部请求、不碰数据。
# 自建 Plausible 实例可同时修改 VITE_PLAUSIBLE_SRC 指向自有域名下的脚本。

# Plausible 域名，例如 resume.example.com（对应 canonical 域名）
VITE_PLAUSIBLE_DOMAIN=

# Plausible 脚本地址（默认官方 CDN；自建时改为 https://your-plausible.example.com/js/script.js）
VITE_PLAUSIBLE_SRC=https://plausible.io/js/script.js
